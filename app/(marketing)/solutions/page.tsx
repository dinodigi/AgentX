import { SolutionsBrowser } from "@/components/marketing/SolutionsBrowser";

export const metadata = {
  title: "Solutions — by use case | Pluggie",
  description:
    "Agencies, AI builders, content sites, membership, commerce — every claim maps to a shipped AgentX capability.",
};

export default function Solutions() {
  return <SolutionsBrowser />;
}
