import { LeafBrowser } from "@/components/marketing/LeafBrowser";

export const metadata = {
  title: "AgentX capabilities — the platform, area by area | Pluggie",
  description:
    "Data modeling, delivery API, authorization, automation, payments, compute, realtime, trust — each with the real mechanic and its honest boundary.",
};

export default function Capabilities() {
  return <LeafBrowser />;
}
